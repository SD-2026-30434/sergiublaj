spring:
  application:
    name: chef-mgmt-mail
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/chef_mgmt_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
server:
  port: 8888
  servlet:
    context-path: '/api'
app:
  mail:
    from: noreply@chefmgmt.com
